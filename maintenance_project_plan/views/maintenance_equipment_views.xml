<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="hr_equipment_view_form_plan" model="ir.ui.view">
        <field name="inherit_id" ref="maintenance_plan.hr_equipment_view_form" />
        <field name="model">maintenance.equipment</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='maintenance_plan_ids']" position="attributes">
                <attribute name="groups">!project.group_project_user</attribute>
            </xpath>
            <xpath expr="//field[@name='maintenance_plan_ids']" position="after">
                <field
                    name="maintenance_plan_ids"
                    nolabel="1"
                    groups="project.group_project_user"
                    context="{
                        'default_equipment_id': active_id,
                        'hide_equipment_id': 1,
                        'default_project_id': project_id,
                        'default_task_id': preventive_default_task_id,
                    }"
                >
                    <tree>
                        <field name="maintenance_kind_id" string="Kind" />
                        <field name="maintenance_team_id" string="Team" />
                        <field name="name" />
                        <field name="start_maintenance_date" string="Start Date" />
                        <field name="interval" />
                        <field name="interval_step" />
                        <field name="duration" />
                        <field name="next_maintenance_date" />
                        <field
                            name="maintenance_plan_horizon"
                            string="P. Horizon period"
                        />
                        <field name="planning_step" string="P. Horizon step" />
                    </tree>
                </field>
            </xpath>
        </field>
    </record>
</odoo>
